import React, { Component } from "react";
import { Button } from "reactstrap";

class CheckBox extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      //   <Button
      //     color="secondary"
      //     onClick={() => this.onCheckboxBtnClick(tag.name)}
      //     active={this.state.cSelected.includes(tag.name)}
      //     size="sm"
      //     key={tag.id}
      //   >
      //     {tag.name}
      //   </Button>
      <p> </p>
    );
  }
}

export default CheckBox;
